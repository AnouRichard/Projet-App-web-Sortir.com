{% extends 'layout.html.twig' %}

{% block title %} Modifier mon mot de passe {% endblock %}



{% block main %}
    <div class="row justify-content-center">
        {% include ('inc/nav.html.twig') %}
    </div>

    <div class="row justify-content-center">
        <h2>Modifier mot de passe</h2>
    </div>

    <div class="row justify-content-center">

        <div class="col-6">
            <div>
                {{ form_start(userForm) }}
                <div class="col-112">
                    <div class="row mt-3">
                        <div class="col-8">{{ form_widget(userForm.motDePasse) }}</div>
                    </div>
                </div>
                {{ form_end(userForm) }}
            </div>
        </div>
    </div>
    </div>

{% endblock %}
