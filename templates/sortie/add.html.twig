{% extends 'layout.html.twig' %}
{% block main %}

    {{ form_start(sortieForm) }}
    <div class="row justify-content-center">
        <div class="col-12">
            <div class="row justify-content-center col-12">
                <h2>Créer une sortie</h2>
            </div>
            <div class="row mt-3">
                <div class="col-6">
                    <div class="row">
                        {{ form_widget(sortieForm.nom) }}
                    </div>
                    <div class="row mt-3">
                        {{ form_row(sortieForm.dateDeDebut) }}
                    </div>
                    <div class="row mt-3">
                        {{ form_row(sortieForm.dateCloture) }}
                    </div>
                    <div class="row mt-3">
                        {{ form_row(sortieForm.nbInscriptionMax) }}
                    </div>
                    <div class="row mt-3">
                        {{ form_row(sortieForm.duree) }}
                    </div>
                    <div class="row mt-3">
                        {{ form_row(sortieForm.descriptionInfos) }}
                    </div>
                </div>
                <div class="col-6">
                    <div class="row mt-3">

                    </div>
                    <div class="row mt-3">

                    </div>
                    <div class="row mt-3">
                        {{ form_row(sortieForm.lieux_no_lieux) }}
                    </div>
                    <div class="row mt-3">

                    </div>
                    <div class="row mt-3">

                    </div>
                    <div class="row mt-3">

                    </div>
                    <div class="row mt-3">

                    </div>
                </div>
            </div>
            <div class="row justify-content-center mt-5">
                <button class="btn btn-lg btn-secondary m-2" type="submit">Enregistrer</button>
                <button class="btn btn-lg btn-secondary m-2" type="button">Publier la sortie</button>
                <button class="btn btn-lg btn-secondary m-2" type="button">Annuler</button>
            </div>
        </div>
    </div>
    {{ form_end(sortieForm) }}
{% endblock %}